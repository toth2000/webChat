import { makeStyles } from "@material-ui/core";

export const useStyles = makeStyles({
  inputGrid: {
    gap: "8px",
  },

  input: {
    width: "70%",
  },

  messageGrid: {
    width: "auto",
    backgroundColor: "#ecf0bd",
  },

  scrollToBottom: {
    padding: '15px',
    height: 500,
    width: 600,
    marginTop: '8px',
    marginBottom: '8px',
  },
});
